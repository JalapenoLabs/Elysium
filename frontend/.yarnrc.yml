nodeLinker: node-modules

# Yarn refuses git dependencies unless their repository is approved. Only the
# org's own packages (@jalapenolabs/cli, @jalapenolabs/uikit) are fetched from git.
approvedGitRepositories:
  - "https://github.com/JalapenoLabs/*"
